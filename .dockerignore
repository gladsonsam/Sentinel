# agent/ is a standalone crate (not a workspace member) – not needed for server builds
agent/

# Rust build artefacts
target/
.xwin-cache/

# Node deps (copied from package.json during Docker build instead)
frontend/node_modules/

# Old server-bundled UI (now built in Dockerfile)
server/static/

# Git / dev files
.git/
*.md
move.sh

# Optional local junk (e.g. leftover before `git pull`); the real `api` module is `server/src/api/`.
server/src/api.rs
